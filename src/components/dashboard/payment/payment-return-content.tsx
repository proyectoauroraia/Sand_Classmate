
'use client';
// This component is now obsolete.
// The logic is handled by the page /src/app/dashboard/payment/return/page.tsx
// which is a server-rendered page that gets status from the URL query params.
// This file can be safely removed in the future.
import * as React from 'react';

export function PaymentReturnContent() {
    return <div>Redirecting...</div>;
}
